name: Vagrant up on FreeBSD 12

on: [push]

jobs:
  vagrant-up:
    runs-on: macos-10.15

    steps:
    - uses: actions/checkout@v2

    - name: Cache Vagrant boxes
      uses: actions/cache@v2
      with:
        path: ~/.vagrant.d/boxes
        key: ${{ runner.os }}-vagrant-${{ hashFiles('freebsd12/Vagrantfile') }}
        restore-keys: |
          ${{ runner.os }}-vagrant-

    - name: Run vagrant up
      run: cd freebsd12; mkdir data; echo "<?php\nphpinfo();\n" > ./data/index.php; vagrant up

    - name: ssh into box after boot
      run: cd freebsd12; vagrant ssh -c "echo 'hello world!'"
